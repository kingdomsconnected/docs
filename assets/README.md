# Assets

Branding used by the generated site. Drop `logo.png` here and add
`"logo": "assets/logo.png"` to `docs.config.json` to show it in the header; the
site renders without one until then.

Images that belong to a single guide live beside that guide's Markdown file
instead, not here.
